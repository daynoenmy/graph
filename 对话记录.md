# 对话与方法记录

## 2026-07-22：V1 方法状态

当前研究重点已经回到 **V1：噪声感知病灶保持 Patch Graph**。V2 暂时不作为当前
代码和论文分析重点，后续只在需要时作为对照实验讨论。

### 1. V1 的方法定位

V1 建立在 AA-CLIP 的异常感知文本—图像对齐框架上，使用空间对齐的模态相关辅助
噪声视图估计 patch 对噪声的敏感程度，再通过病灶保持图结构传播可靠上下文。

V1 不使用：

- 扩散模型；
- 生成式图像去噪；
- 知识蒸馏；
- 测试阶段病灶 mask。

辅助视图不是“去噪后的干净图”，而是用于估计不确定性的强度扰动视图。

### 2. V1 的核心流程

```text
正常/异常文本提示
      │
      ▼
冻结的 CLIP Text Encoder + Text Adapter
      │
      └────────> 正常/异常文本锚点

原始医学图像 I ─────────────> 主视图特征 P
      │
      └─> 模态相关噪声视图 ─> 辅助视图特征 R（No Gradient）

P、R、文本锚点
      │
      ├─> 图输入 G=(P+R)/2
      ├─> Patch不确定性 u=(1-cos(P,R))/2
      └─> 文本引导异常先验 a
                       │
                       ▼
          噪声感知病灶保持 Patch Graph
                       │
                       ▼
              多尺度 Pixel Anomaly Map ─> max ─┐
                                                    ├─> Medical Image Score
CLIP 最终 CLS ─> CLS—图特征融合 ─> Global Score ─┘
```

### 3. 医学模态辅助噪声

当前实现根据数据集选择模态启发的扰动：

| 数据集 | 辅助噪声机制 |
|---|---|
| Brain | Rician 幅值噪声 + MRI bias field |
| Liver | CT 信号相关量子噪声近似 |
| DDTI | 超声 speckle + 弱电子噪声 |
| Retina | shot noise + 弱加性噪声 |
| Colon | 光照变化 + 弱加性噪声 |

这些噪声是强度扰动近似，不应在论文中表述为完整或精确的真实设备物理模型。

### 4. V1 图结构

V1 使用两种邻接关系：

- Hard KNN 语义图；
- patch 网格空间八邻域图。

混合拓扑为：

\[
A=\alpha A_{\mathrm{KNN}}+(1-\alpha)A_{\mathrm{spatial}}.
\]

当前默认：

```text
K = 8
alpha = 0.7
graph residual weight = 0.2
```

最终边权同时考虑：

- patch特征亲和度；
- 消息来源节点的噪声可靠性 \(1-u_j\)；
- 正常/异常概率差异对应的病灶边界亲和度。

高噪声来源节点的外传信息被削弱；正常与疑似病灶之间的跨边界传播被削弱。

### 5. 病灶保护更新门

V1 使用节点级更新门：

\[
q_i=\sigma(s_u u_i-s_a a_i+b).
\]

精炼特征为：

\[
F'_i=(1-q_i)G_i+q_iZ_i.
\]

解释为：

- patch不确定性越高，越需要请求可靠邻居上下文；
- patch异常概率越高，图更新越少，从而保留疑似病灶自身特征；
- 边界亲和度和训练期边界损失共同减少病灶过平滑。

### 6. V1 训练损失

总目标包括：

\[
\mathcal L=
\mathcal L_{\mathrm{cls}}
+\mathcal L_{\mathrm{seg}}
+\lambda_c\mathcal L_{\mathrm{cons}}
+\lambda_p\mathcal L_{\mathrm{pres}}
+\lambda_b\mathcal L_{\mathrm{boundary}}.
\]

- `L_cls`：图像分类交叉熵；
- `L_seg`：多尺度 Focal + Dice；
- `L_cons`：主视图/辅助视图正常—异常预测的对称一致性；
- `L_pres`：训练 mask 内病灶特征保持；
- `L_boundary`：训练 mask 边界两侧的特征对比约束。

当前 `train.bat` 使用：

```text
noise consistency weight = 0.10
lesion preservation weight = 0.10
boundary contrast weight = 0.05
boundary margin = 0.20
```

训练 mask 只进入源域训练损失，不进入目标域图传播和推理。

### 7. 当前代码的实际测试逻辑

当前工作区为 V1-only 版本，需要特别记住：

1. `test.py` 当前固定加载 `image_adapter_2.pth`，不会自动测试第一轮或全部模型；
2. `--noise_severity 0.06` 只控制内部辅助噪声视图；
3. 当前主测试输入没有额外添加噪声；
4. 当前测试设置是“干净主输入 + 目标模态辅助噪声探针”；
5. 医学 Image Score 现为 Pixel Anomaly Map 最大值与全局融合分数的加权和；
6. `det_feature` 现已融合第 24 层 CLIP 最终 CLS，不再被医学
   `metrics_eval()` 完全覆盖；
7. 当前 `visualize()` 只实现了 MVTec，不能直接用于 Liver case 图。

原 V1 实验中，Liver 上的 Image AUC 提升应解释为：

> V1 改善了每张图像最大 patch 异常响应在正常/异常图像之间的排序。

不能直接归因于全局分类分支。这些旧结果不能冒充加入 CLS 后的新结果。

### 7.1 2026-08-01：加入 CLIP 最终输出

当前代码已实现两项改动：

1. 从主视图第 24 层保留 CLS token，经 CLIP 的全局池化、`ln_post`和
   视觉投影得到最终全局特征 (C)；
2. 图检测特征与 CLS 按 `clip_global_weight=0.2` 融合；
3. 医学 Image Score 按 `medical_image_score_global_weight=0.2` 融合热图最大值
   和全局异常分数；
4. Pixel Anomaly Map 路径保持不变，因此 CLS 不直接破坏局部定位；
5. 权重设为 0 可恢复原 V1 行为，旧 checkpoint 不需要新参数即可加载。
6. 新 checkpoint 会记录 `clip_global_weight`，测试与可视化时会检查权重是否一致。

新实验使用 `ckpt/noise_graph_cls`，不覆盖原来的 `ckpt/noise_graph`。
`clip_global_weight` 和 `medical_image_score_global_weight` 必须在源域验证协议下固定，
不能按 Liver、DDTI 等目标测试集分别调整。

### 8. 当前实验观察

已观察到：

- V1 第一轮在 Liver 上效果最好；
- V1 第一轮在 DDTI 上同样最好；
- Liver 上，在 Pixel AUC 不下降的同时，Image AUC 相比 AA-CLIP 提高约 13 个百分点。

其中“13%”应根据原始数值区分：

- 例如 70% 提高到 83%，应写“提高 13 个百分点”；
- 只有相对增幅计算结果为 13% 时，才写“相对提高 13%”。

该结果在用于论文前仍需确认：

- AA-CLIP 与 V1 使用相同测试样本；
- 使用相同输入设置和评估代码；
- checkpoint 由验证协议预先确定；
- Pixel AP 和 Image AP 没有出现相反变化；
- 多随机种子结果具有稳定性。

### 9. 第一轮效果更好的原因判断

Brain `full-shot.jsonl` 当前约有 3715 个样本，默认图像 batch size 为 2，所以一轮约
包含 1858 次优化更新。V1 图像部分约有千万级可训练参数，默认学习率为 `5e-4`，
默认训练 20 轮。

因此，第一轮已经不是“小幅更新”，而可能已经完成必要的医学适配。后续训练可能：

- 进一步拟合 Brain 特有纹理和病灶形态；
- 拟合训练增强和辅助噪声分布；
- 使 Adapter 输出偏离 CLIP 的通用表示；
- 降低 Liver、DDTI 等目标模态的跨域泛化；
- 增强图传播造成的小病灶过平滑。

如果 Brain 后期提高而 Liver/DDTI 下降，主要是源域过拟合；如果三者都下降，则还要
检查学习率过大、损失权重和图传播过强。

### 10. 论文使用原则

- V1 当前是论文主网络候选；
- AA-CLIP 应作为基础对照；
- V2 暂时只作为后续鲁棒性对照，不替代 V1；
- 不按不同目标测试集分别选择最优 epoch；
- 使用源域验证集或预先固定协议选择统一 checkpoint；
- 同时报告 Image AUC/AP 和 Pixel AUC/AP；
- 报告干净输入、噪声输入、跨数据集和多随机种子结果；
- case study 用于解释整体结果，不能替代数据集级统计证明；
- 论文中不能宣称对任意未知噪声具有严格理论保证。

### 11. 已生成的 V1 文件

- 详细方法总结：[V1_METHOD_SUMMARY.md](V1_METHOD_SUMMARY.md)
- Mermaid 方法图：[V1_METHOD_DIAGRAM.md](V1_METHOD_DIAGRAM.md)
- 具体病例分析脚本：[case_analysis.py](case_analysis.py)
- V1 同步带噪图像生成脚本：[generate_noisy_images.py](generate_noisy_images.py)
- V1 Patch 状态可视化脚本：[visualize_patch_states.py](visualize_patch_states.py)

`case_analysis.py` 会公平比较原始 AA-CLIP 与 V1，输出所有病例分数、数据集级
Image AUC/AP、代表性改善病例、小病灶病例、正常误报抑制病例和失败病例，并生成
原图、GT、AA-CLIP热图、V1热图与V1不确定性图。

`generate_noisy_images.py` 直接调用 V1 训练中的
`utils.make_medical_noise_view()`，从 `dataset/constants.py` 读取数据路径，
并输出干净图、V1 辅助噪声视图、差异图、对比图和 `manifest.csv`。
默认噪声强度为 `0.06`，同一原始文件和种子始终生成同一个噪声实现。

`visualize_patch_states.py` 在真实 V1 checkpoint 和真实医学图像上提取
Patch 不确定性、异常概率、干净/噪声预测分歧、源节点可靠性、图更新门和
精确的图转移权重，并将其事后解释为四种 Patch 状态。四种颜色只用于可视化，
不是 V1 内部的四分类头，也不会修改模型预测。

### 12. V1 的推荐名称

中文：

> 噪声感知病灶保持 Patch 图适配网络

英文：

> Noise-Aware Lesion-Preserving Patch Graph Adaptation

推荐缩写：

> NLPG-Adapter

## 13. 2026-08-02：V1 引入离线 LLM 文本描述

用户希望正常/异常文本由 LLM 生成，并要求按此前建议修改代码。为保证医学实验可复现，
当前实现采用**离线 LLM Prompt Bank**，而不是在每个 batch 中在线请求模型：

- 新增 `dataset/llm_prompts.json`，为 Brain MRI、Liver CT、DDTI 超声、Retina
  眼底和 Colon 内镜分别提供 6 条正常、6 条异常的可见外观描述；
- 描述只涉及强度、纹理、边界、对称性和局部结构等图像可观察线索，避免把具体诊断
  当作已知事实；
- `train.py`、`test.py`、`case_analysis.py` 与 `visualize_patch_states.py` 使用同一
  Prompt Bank；
- 新 checkpoint 保存 `prompt_source`、Prompt Bank SHA256 和 schema version；测试时
  文本来源或文件哈希不一致会直接报错；
- 旧模板文本 checkpoint 必须使用 `--prompt_source template`，不能与新 LLM 文本
  checkpoint 混用；
- 新实验保存到 `ckpt/noise_graph_cls_llm`，不会覆盖原 `noise_graph_cls`。

同时修正全局分数的语义计算：

\[
S_{\mathrm{global}}
=\operatorname{Softmax}
\left(\tau Z_{\mathrm{img}}^\top[t_n,t_a]\right)_{a},
\]

默认 `global_text_temperature=10.0`。这会让正常文本与异常文本直接竞争，不再只读取
异常文本余弦相似度。训练分类损失使用同一组温度缩放 logits，训练与测试保持一致。

`test.py` 还恢复了 `--image_checkpoint` 参数；默认
`image_adapter_*.pth`，会按 epoch 数值顺序评估所有编号 checkpoint。

论文中应把模板文本与 LLM 文本作为独立消融项。LLM 文本实验需要重新训练，不能把
此前模板文本得到的 Liver、DDTI 或 Brain 指标直接标记为 LLM 版本结果。

## 14. 2026-08-02：独立 V3 冻结 Encoder 空间—频率图

用户决定放弃继续向 V1 Encoder 内增加 Adapter，希望采用近期工作常见的冻结基础
Encoder 范式，并在空间—频率变换与图建模上创新。当前实现保留 V1 作为对照，新建
独立 `V3` 分支和独立训练测试入口。

V3 完全冻结 CLIP 图像和文本 Encoder，只训练 Encoder 后约 2.6 万参数的
Spatial-Frequency Coherence Head。冻结 patch 网格经过固定平稳 Haar 小波得到低频结构
和三个方向高频能量，再以固定八邻域构造空间—频率图。Head 联合固定文本异常先验、
邻域异常一致性、语义图残差和频率图残差，对冻结 CLIP margin 做残差修正。

V3 删除 V1 的 Text/Image Adapter、双噪声图像分支、KNN 图、不确定性门、病灶/边界
损失和 CLS 分数融合。频带一致性训练只衰减 Head 内一个高频子带，Encoder 每张图只
运行一次。静态二维医学图像应称为空间—频率而不是时间—频率。

新增文件为 `model/frozen_sfgraph.py`、`v3_utils.py`、`train_v3.py`、`test_v3.py`、
`train_v3.bat`、`test_v3.bat` 和 `V3_METHOD.md`。V3 checkpoint 保存 Prompt Bank
哈希、冻结声明与全部架构参数，测试时会严格检查。

## 15. 2026-08-02：V3.1 病灶保持图增强

针对“冻结 Encoder、小波和图本身已存在相近工作”的创新性风险，将 V3 的核心机制
强化为病灶保持双阶段空间—频率图。第一阶段使用不含中心自环的邻域异常共识，分别
构造病灶支持门与孤立伪异常证据；第二阶段只在节点同时获得自身和邻域异常支持时
抑制跨语义消息。这样，连续病灶减少边界平滑，而孤立高频伪异常仍可由相容邻居修正。

残差输出改为有界 `tanh` 修正，默认满足 `|score - frozen_margin| <= 4`，并保持末层
零初始化。频带训练从单向衰减升级为 `[0.5, 1.5]` 双向缩放：正常区域约束稳定，病灶
区域使用单边保持损失防止异常响应被频率干预抹除。checkpoint 升级为 version 2，
旧 version 1 V3 Head 因结构不同不能复用，需要重新训练。
