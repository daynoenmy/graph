# V4.2-SSC：语义方面条件化的图拉普拉斯谱残差

> 状态：已实现。
> 基础版本：V4.1 `FrozenModalityGraphSpectralModel`。
> 定位：替代 V4.2-Lite 中"可学习 Prompt 加权 + Rank-4 文本残差"的文本增强方案，将创新点落在
> 文本与图拉普拉斯谱阶的耦合上，而不是再叠加一个小型文本网络。

## 1. 动机：为什么不做加权和 LoRA

原 V4.2-Lite 的文本模块包含三部分：解剖—模态特异 Prompt（内容改动）、共享可学习
Prompt 加权（softmax 权重）、共享 Rank-4 文本残差（LoRA 式适配器）。

这三部分都不构成创新点：

| 组件 | 对应已有工作 |
|---|---|
| Prompt 内容换词 | 工程改进，非方法创新 |
| 多 Prompt 加权/集成 | WinCLIP 的 Prompt Ensemble |
| 可学习 Prompt 选择 | CoOp 的 Prompt Learning（但按数据集学，破坏 LODO） |
| 低秩文本残差 | LoRA / 文本 Adapter |

此外，同一状态内多条 Prompt 特征高度相关，软max 加权的梯度信号弱，训练后大概率
仍接近均匀分布——"加权"可能只是增加了 8 个不动的参数。

V4 系列的独有资产是**图拉普拉斯谱残差融合**（`m`、`Lm`、`L²m`）。文本模块的创新
因此应长在**文本 ↔ 谱阶的耦合**上：让文本语义决定使用哪一阶拉普拉斯残差，而不是
让文本决定用哪条 Prompt。

## 2. 核心思想：学习语义方面与谱阶的对应关系

不同异常方面可能需要不同的图拉普拉斯修正，但不预先硬编码其对应关系。对于图
拉普拉斯特征值 \(\lambda\)，\(L\) 和 \(L^2\) 的响应分别为 \(\lambda\) 和
\(\lambda^2\)，所以 \(L^2\) 相对更强调高图频变化，不能简单解释为“弥漫低频”。

V4.2-SSC 只固定三个互补语义方面：局灶、弥漫和结构异常。一个跨层、跨数据集共享的
\(3\times2\) 小矩阵根据源域监督学习这三个方面如何修正一阶、二阶残差。冻结 CLIP
全局特征只用于计算图像与三个方面原型相对于正常锚点的语义兼容度。这些兼容度不是
校准后的病变比例，而是**全局实例级**条件信号，不是 Patch 级空间条件。

## 3. 方法

### 3.1 冻结方面原型

从 Prompt Bank 中选出三条互补的异常描述作为方面原型：

```text
p_focal   = "a {} with a focal lesion"
p_diffuse = "a {} with a diffuse abnormality"
p_struct  = "a {} with structural distortion"
```

其中 `{}` 填入解剖—模态基础名称（`brain MRI scan`、`liver CT scan`、
`retinal OCT scan` 等）。冻结 CLIP Text Encoder 编码并归一化：

\[
e_a=\operatorname{Norm}(E_{text}(p_a)),
\qquad a\in\{focal, diffuse, structural\}.
\]

### 3.2 相对正常锚点的独立方面兼容度

使用 V4.1 已计算的冻结 CLIP 最终全局特征 \(g\)（CLS 路径，不增加任何前向），
计算每个方面相对于正常锚点 \(t_n\) 的兼容度：

\[
q_a=\sigma\left(
\tau_m[\langle g,e_a\rangle-\langle g,t_n\rangle]
\right).
\]

- 计算只涉及冻结特征与固定原型的内积，**不增加可训练参数**；
- 独立 sigmoid 允许多个方面同时激活，也允许正常图像的三个方面同时较低；
- 该估计来自图像自身内容，不读取目标域标签或 Mask，符合严格 LODO；
- 混合温度 \(\tau_m\) 默认固定，必须在源域验证协议下统一选择，不能按目标域调整。

### 3.3 方面调制谱残差

设 V4.1 模态条件器输出的每层谱 Logit 为
\(z_{l,k}^{mod}=(W_\beta t_m+b_\beta)_{l,k}\)。将兼容度中心化并通过共享耦合矩阵
\(C\in\mathbb R^{3\times2}\) 生成实例修正：

\[
\Delta z_k=\sum_a(q_a-\tfrac12)C_{a,k},
\qquad k\in\{1,2\}.
\]

最终有界谱系数为：

\[
\beta_{l,k}=\beta_{max}\tanh
\left(z_{l,k}^{mod}+\rho\Delta z_k\right).
\]

最终异常图为：

\[
S=\sum_l\alpha_l\left[
m^{(l)}
+\beta_{l,1}Lm^{(l)}
+\beta_{l,2}L^2m^{(l)}
\right].
\]

正式方法固定 \(\rho=1\)，即启用语义—谱耦合；\(\rho=0\) 只用于无耦合消融。
不把 \(\rho\) 作为可按目标域调节的连续超参数。

- \(C\) 由源域监督学习语义方面与谱阶的对应关系；
- \(C\) 在四个特征层之间共享，避免增加层专属参数；
- 加法发生在 `tanh` 之前，不会像乘法门控那样无条件缩小 V4.1 残差梯度；
- 所有谱系数仍保持有界、有符号。

### 3.4 零初始化严格等价

\(W_\beta,b_\beta,C\) 全部零初始化，因此初始 \(\beta=0\)，模型初始状态严格等于
使用相同解剖—模态特异锚点的四层 CLIP Margin 均值，即消融 A1。它与原 V4.1 的文本
内容仍有差异，该差异由 A0→A1 单独度量。耦合矩阵在第一步即可接收来自谱残差的梯度。

## 4. 参数预算

| 模块 | 可训练参数 |
|---|---|
| V4.1 模态条件图融合器 | 9,228 |
| 共享方面—谱阶耦合矩阵 | **6** |
| 可选共享标量混合温度 \(\tau_m\)（log 参数化） | 1 |

默认总可训练参数为 **9,234**。新增参数只负责学习三个冻结语义方面如何修正两个谱阶，
不增加视觉或文本编码器前向。

## 5. 对 V4.1 悬案的回应

原 V4.2-Lite §8 要求正式实验比较 `L` 与 `L+L²`，如果二阶残差没有稳定收益就删除
`L²`。修正版不预设 `L²` 对应弥漫异常。正式实验仍必须比较 `L` 与 `L+L²`；若
`L²` 没有稳定收益，应删除二阶分支。只有当学习到的耦合矩阵和消融共同显示二阶分支
存在稳定、可重复的语义条件收益时，才保留 `L²`。

## 6. 严格 LODO 合规

1. 目标数据集不参加参数训练；
2. 方面原型和基础名称在训练前固定，内容与顺序记录哈希；
3. 方面兼容度只使用冻结 CLIP 全局特征、正常锚点与固定原型，不读取目标图像标签或 Mask；
4. 混合温度 \(\tau_m\) 由源域验证协议统一选择；
5. 与 V4.1 相同的 checkpoint 校验与哈希机制。

## 7. 消融设计

| 编号 | 文本锚点 | Prompt 融合 | 谱耦合 | 图残差 | 目的 |
|---|---|---|---|---|---|
| A0 | 原始通用文本 | 均匀平均 | 无 | 无 | 冻结 CLIP 基线 |
| A1 | 解剖—模态特异文本 | 均匀平均 | 无 | 无 | 验证 Prompt 内容 |
| A2 | 解剖—模态特异文本 | 可学习加权 | 无 | 无 | 原加权方案（预期≈A1） |
| A3 | 解剖—模态特异文本 | 均匀平均 | ✓ | 仅 `L` | 验证一阶耦合 |
| A4 | 解剖—模态特异文本 | 均匀平均 | ✓ | `L+L²` | **默认方法** |
| A5 | A4 + 可学习加权 | 可学习加权 | ✓ | `L+L²` | 加权是否还有增量 |
| A6 | A4 + 器官正交约束 | 均匀平均 | ✓ | `L+L²` | 方向 B 约束合并 |
| A7 | A4 + Rank-4 适配器 | 均匀平均 | ✓ | `L+L²` | 验证适配器非必要 |

所有结果至少 3 个随机种子，报告 Image/Pixel AUC、AP、异常 Mask 覆盖率，以及
方面兼容度的总体均值/标准差、正常/异常均值差、饱和率和两两相关系数。训练和测试
还记录三个原型的原始两两余弦、相对正常锚点方向的两两余弦及方向范数。还必须加入
常数条件、打乱方面名称和 \(\rho=0\) 无耦合对照，排除收益仅来自普通正则化。

## 8. 可解释性

方面兼容度 \(q_{focal},q_{diffuse},q_{struct}\) 提供中间语义证据，可作为：

- 谱阶选择的中间证据（机制解释）；
- 数据集级方面响应统计，但不能解释成经过校准的病变比例；
- 失败案例分析工具（某图像被判错时，检查其方面兼容度是否合理）。

## 9. 创新主张与对照定位

一句话主张：

> 我们使用 6 参数共享耦合矩阵，让冻结的全局实例级异常方面兼容度对模态条件图拉普拉斯
> 残差进行有界修正，同时保持冻结 CLIP 和严格 LODO。

对照定位：

- 与 WinCLIP（冻结集成）、CoOp（按数据集学 Prompt）、PromptAD/AA-CLIP（学锚点）
  不同，本方法既不学习 Prompt 也不学习锚点，而是学习"文本与谱的交互结构"；
- 与其他方法的差异点是：冻结编码器 + 跨域不记忆 + 文本引导谱阶选择；
- 只增加 6 个共享参数、严格 LODO 不放松，是该方法区别于大型文本适配器的主要特点。

## 10. 已知风险与对策

| 风险 | 对策 |
|---|---|
| 冻结 CLIP 全局特征与方面原型对齐不足 | 语义主干 `m` 不受影响；报告兼容度统计并与无耦合版本比较 |
| 正常图像对异常方面仍有高兼容度 | 使用相对正常锚点的独立 sigmoid，并分别报告正常/异常样本统计 |
| 三个方面原型近共线、有效自由度不足 | 报告原始/相对正常方向余弦及兼容度相关性；默认不做 Gram–Schmidt，避免人为改变冻结 CLIP 语义几何 |
| 全局条件可能低估小病灶 | 明确限定为全局实例级耦合；Patch 级条件只作为后续扩展，不属于默认轻量模型 |
| 语义耦合只起普通正则化作用 | 增加常数条件、打乱方面名称和无耦合对照 |

## 11. 一句话总结

> V4.2-SSC 用冻结 CLIP 的全局特征计算局灶/弥漫/结构异常相对正常锚点的兼容度，再由
> 6 参数共享矩阵学习这些语义方面如何修正 `Lm` 与 `L²m` 的有界系数，在保持四层
> CLIP Margin 零初始化基线和严格 LODO 的同时实现全局实例级文本—谱阶耦合。
