# V4.2-Lite 设计稿评审

> 评审对象：[V4_2_LITE_TEXT_METHOD.md](V4_2_LITE_TEXT_METHOD.md)
> 参照方法：V4.1 `FrozenModalityGraphSpectralModel`（`V4_METHOD.md`）
> 评审基准：仅以 V4 版本系列的设计原则为准，不涉及 V3。

## 1. 总体结论

在 V4 框架内，V4.2-Lite **基本合理**，方法骨架与 V4.1 的原则一致：

- 完全冻结 CLIP Image/Text Encoder，不新增大型网络；
- 默认新增参数约 9.2K（V4.1 的 9,228 + Prompt 权重），符合参数高效路线；
- 权重零初始化，训练起点为均匀平均，延续 V4.1"初始严格等价、只能做有界修正"的设计哲学；
- Prompt 权重跨数据集共享、不按目标域学习，保持严格 LODO 无泄漏；
- Prompt 固定 + 内容哈希 + checkpoint 校验，训练/测试不调用 LLM，与 V4.1 一致；
- A0–A5 消融阶梯完整，能逐项分离 Prompt 内容 / 加权 / 图残差的贡献，并强制多种子 + 源域验证选 checkpoint。

但存在 **3 处实质性问题需要修正**，以及 **2 处次要问题建议补充**。

## 2. 需要修正的问题

### 2.1 参数量自相矛盾（§1、§5、§11）

文档列出的 Prompt 集为 3 条正常 + 5 条异常，共 **8 个可学习 logit**，而非文中多处声称的"约 16 个可训练参数"。

- 方案 A：将"约 16"全部改为"8"；
- 方案 B：将示例 Prompt 扩充为每类 8 条，再声称 16。

该数字属于可被抽查的事实，必须与代码中的 Prompt Bank 一致。

### 2.2 "初始锚点严格等于 V4.1 基线"表述不准确（§5.2、§15）

V4.1 使用仓库旧模板文本，V4.2-Lite 使用解剖—模态特异 Prompt，Prompt 内容本身已改变。

因此零初始化只能保证训练起点**严格等于消融 A1（新 Prompt + 均匀平均）**，不能声称"严格等于 V4.1"。

建议改为：

> V4.2-Lite 的零初始化状态与 A1 严格等价；与 V4.1 的差异来自 Prompt 内容本身，由 A0→A1 消融单独度量。

### 2.3 Retina 的 base name 写错（§4 表格）

BMAD 的 `Retina` 数据集是**眼底彩照（fundus）**，不是 OCT；`retinal OCT scan` 只适用于 `Retina_OCT2017`。

该错误从 V4.1 模态模板表延续下来，但在 V4.2-Lite 中后果更严重——base name 直接进入正常/异常文本锚点，而不仅仅是模态条件向量。

建议改为：

| 数据集 | 推荐基础名称 |
|---|---|
| Retina | `retinal fundus image` |
| Retina_OCT2017 | `retinal OCT scan` |

## 3. 建议补充的说明

### 3.1 Prompt 权重的可辨识性与解释性措辞（§5.2）

同一状态内的多条 Prompt 特征高度相关，8 个 softmax 权重的梯度信号弱，训练后可能长期接近均匀分布。

- 建议弱化"直接解释不同正常/异常描述的贡献"的表述；
- 改为在附录报告：训练后权重值、权重熵、多种子权重稳定性；
- 若权重最终接近均匀（A2≈A1），按 V4.1"更简单优先"的原则保留 A1 即可，这本身是可接受结论。

### 3.2 无 Mask 源域下的锚点监督方向（§11、§12）

严格 LODO 中，当 Chest、Retina_OCT2017、Histopathology 这类无异常 Pixel Mask 的数据集出现在源域时，它们只提供图像级 BCE 梯度，文本锚点的像素级定位方向依赖其他有 Mask 源域。

- 建议在文档中显式承认该现象；
- 正式实验中观察：加入加权后，无 Mask 源域测试目标（如留出 Chest）的 Pixel AUC 是否受影响；
- 不建议为此引入额外约束，保持 V4.1 的简洁性。

## 4. 无需改动的部分

- V4.1 视觉主干与图融合主线完全保留；
- 可选共享 Rank-4 文本残差（γ 零初始化、约 6K 参数）仅作消融、默认关闭；
- checkpoint 保存 Prompt 顺序与 Prompt Bank 哈希，训练/测试严格校验；
- 模态条件化有界融合、固定局部读出、损失结构均沿用 V4.1；
- LODO 协议（不读目标图像/Mask/标签、源域验证选 checkpoint、统一超参规则）保持严格。

## 5. 一句话总结

> V4.2-Lite 的设计与 V4.1 原则兼容、合理；修正"8 个 logit 而非约 16 个"、初始等价对象应为 A1、Retina 应为 fundus 三处表述后，即为可进入实现与消融实验的严谨设计稿。
