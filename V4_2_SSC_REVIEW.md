# V4.2-SSC 设计评审：合理性与创新性

> 评审对象：[V4_2_TEXT_SPECTRAL_COUPLING.md](V4_2_TEXT_SPECTRAL_COUPLING.md)（修正版）
> 参照方法：V4.1 `FrozenModalityGraphSpectralModel`
> 评审日期：2026-08-08

## 1. 总体结论

修正版已解决初版的核心问题（谱语义硬编码、正常图像无约束、乘法门控衰减、等价表述
错误），内部自洽且与 V4 原则一致。但仍有 5 个合理性问题（3 个需修改、2 个需明确）
和 3 个创新性局限需要处理。

## 2. 已正确修正的点（不需再动）

| 问题 | 修正情况 |
|---|---|
| `L²` 被硬编码对应弥漫异常 | §2 明确 `L²` 强调高图频，不预设对应关系；§5 要求无稳定收益即删除 |
| 正常图像被 softmax 强制分配方面 | 改为相对 `t_n` 的独立 sigmoid，可同时低 |
| 乘法门控无条件缩小残差 | 改为 `tanh` 前加法耦合 |
| "初始等价 V4.1"表述错误 | 改为等价消融 A1，差异由 A0→A1 度量 |
| 收益可能只是普通正则化 | 已加常数条件、打乱名称、无耦合三对照 |

## 3. 合理性问题

### 3.1 原型近共线 → 耦合可辨识性弱（高严重度）

`e_focal`、`e_diffuse`、`e_struct` 共享相同主干前缀（`"a brain MRI scan with ..."`），
CLIP 文本空间中两两余弦很可能 >0.9。后果：

- 三个 `q_a` 高度相关，`C ∈ R^{3×2}` 的 6 个参数梯度方向近乎相同；
- `Δz_k` 退化为近似标量偏移，"学三个方面如何修两个谱阶"实际只有约一维自由度；
- 需要回答"3×2=6"是否虚高，真实自由度数可能只有 1–2。

**修改建议**：
- 在方法章节要求报告三个原型两两余弦相似度；
- 若余弦 >0.9，将原型相对 `t_n` 做 Gram–Schmidt 正交化后再计算 `q_a`，或改用更互补
  的方面措辞；
- 正交化版本应作为默认实现，而不是可选项。

### 3.2 `ρ` 未定义（需明确）

`β = β_max tanh(z^{mod} + ρΔz_k)` 中 `ρ` 没有给出取值方式和取值范围：

- 是超参数还是并入 `C` 学习？
- 若是超参数，由谁按什么协议确定？

**修改建议**：明确 `ρ` 的确定方式。推荐并入 `C`（避免新增旋钮），或作为源域验证
协议统一固定的有界超参数，不能按目标域调整。

### 3.3 实例信号是全局的，不是空间自适应的（需说明）

`q_a` 与 `z_cls` 来自**同一个冻结全局特征 `g`**——图像级分数与谱调制共用一条信息
通道。后果：

- 大片正常组织中小病灶的图像，全局 `q_a` 会低估局灶分量；
- 恰好在最需要 `Lm` 修正的小病灶图像上，残差被关小；
- "实例级"成立，但只是"全局实例级"，不是 patch 级自适应。

**修改建议**：
- §9 表述改为"全局实例级文本—谱耦合"；
- 注明 per-patch 兼容度作为扩展/消融项（冻结前向成本低：与 3 个原型的内积）。

### 3.4 空结果风险需要前置证据（需明确）

若 `q_a` 分离度不足，`C` 训练后仍 ≈0，A4≈A1。消融表能暴露此结果，但无法区分
"耦合机制无效"与"原型/温度选得差"。

**修改建议**：把"方面兼容度分布统计"从 §10 的风险对策升为**必报项**，且必须按
正常/异常样本分别报告（如各数据集 `q_{focal}` 的均值差与重叠度）。

### 3.5 残留表述错误（需修正）

- §3.1 示例基础名称仍写 `retinal OCT scan`：BMAD `Retina` 是眼底彩照（fundus），
  `retinal OCT scan` 只属于 `Retina_OCT2017`，应改为 `retinal fundus image`；
- 该错误从 V4.1 模态模板表延续，但在本方法中直接进入方面原型文本，影响更大。

## 4. 创新性问题

### 4.1 真实成立的创新点

1. **约束组合**：冻结 CLIP + 6 参数 + 严格 LODO 下学习"文本—谱交互结构"，而非
   Prompt/锚点/残差本身——对比 WinCLIP（集成）、CoOp（按数据集学 Prompt）、
   PromptAD/AA-CLIP（学锚点）、LoRA（学残差），定位清晰且依赖 V4 独有谱融合框架，
   不易被移植；
2. **无学习自适应**：`q_a` 推理时由图像自身计算，文本先验逐图像变化，不同于静态锚点；
3. **可解释副产品**：方面兼容度可作为数据集级异常形态统计与失败案例分析工具。

### 4.2 诚实的局限

| 局限 | 说明与对策 |
|---|---|
| 组合型而非新算子 | 元件均为已知（CLIP 相似度 + sigmoid + tanh 有界残差 + 共享线性耦合），新的是交互拓扑与约束域；论文表述应为"结构组合创新" |
| 6 参数的学习强度弱 | "学习方面—谱阶映射"的说法过强；故事核心是"语义结构化的条件信号"，应避免声称学到了可解释映射 |
| 实证依赖强 | 若 `q_a` 信号弱，创新点实证塌陷到只剩 A1（工程内容）；§9 主张必须由 §7 三对照 + 兼容度统计实证支撑 |
| 与已有工作间接关联 | GNN 条件/动态谱滤波、CLIP 条件调制均有关联；需查证后限定表述："据我们所知，在冻结 CLIP 医学严格 LODO 下首次"，不能写通用"首次" |

## 5. 修改清单（按优先级）

| 优先级 | 问题 | 动作 |
|---|---|---|
| 高 | 原型近共线 | 报告两两余弦；默认 Gram–Schmidt 正交化 |
| 高 | `ρ` 未定义 | 明确并入 `C` 或源域验证固定 |
| 中 | 全局信号局限 | 表述改为"全局实例级"；补 per-patch 扩展 |
| 中 | 空结果风险 | 兼容度分布（正常/异常分列）升为必报项 |
| 低 | Retina 表述 | `retinal OCT scan` → `retinal fundus image` |

## 6. 结论

**合理性**：修正版自洽、符合 V4 原则，剩余问题均为可控改进，不推翻设计；修改
3.1–3.5 后可进入实现。

**创新性**：中等、可辩护，但实证依赖强。建议顺序：先跑 A1 vs A4 对比与兼容度
分布统计，三对照通过后再定论文创新主张。
