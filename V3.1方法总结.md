# V3.1：病灶保持空间—频率图异常检测方法

## 1. 方法概述

V3.1 是一种基于冻结 CLIP 的轻量医学图像异常检测方法。该方法以固定的正常—异常
医学文本语义作为异常先验，在 CLIP Patch 特征上构建空间—频率图，并通过病灶保持
约束学习对基础异常响应的有限修正。

V3.1 的核心思想是：真实病灶通常同时具有异常文本语义、局部空间不一致性和频率结构
变化，而噪声伪异常往往只在其中一个方面表现异常。因此，模型不将高频响应直接视为
病灶，而是联合文本、空间和频率信息判断异常。

整体流程为：

```text
医学图像
   ↓
冻结 CLIP Image Encoder
   ↓
第 6/12/18/24 层 Patch 特征
   ├── 固定正常/异常文本语义
   ├── 八邻域空间关系
   └── Haar 低频与三方向高频特征
                  ↓
       空间—频率图 Head
                  ↓
       分层 Patch 异常概率
          ├── 受约束概率融合 → Pixel 异常定位
          └── Top-k 局灶证据 + GeM 弥漫证据 ─┐
冻结 CLIP 最终 CLS + 正常/异常文本语义 ──────┴→ Image 异常分数
```

## 2. 冻结视觉特征与医学文本先验

给定医学图像 \(I\)，V3.1 使用冻结的 CLIP Image Encoder 提取四个层级的 Patch
特征：

\[
F^{(l)}=E_{img}^{(l)}(I),
\qquad l\in\{6,12,18,24\}.
\]

各层特征独立经过同一个共享参数的空间—频率图 Head。每层 Logit 先转换为概率，再用
非负、和为 1 的可学习权重融合。默认保留 20% 均匀权重质量，因此四层中每层至少占
5%，避免某个尺度被完全关闭。浅层提供纹理和边缘信息，中层提供局部结构信息，深层
提供正常—异常语义信息，同时保持图 Head 参数量不随层数增加。

正常和异常医学描述由冻结的 CLIP Text Encoder 编码。同一状态的多条文本特征经过
归一化、平均和再次归一化，形成固定的正常与异常文本锚点：

\[
t^s=\operatorname{Norm}\left(
\frac{1}{K}\sum_{k=1}^{K}
\operatorname{Norm}(E_{text}(p_k^s))
\right),\quad s\in\{normal,abnormal\}.
\]

每个 Patch 的基础异常语义分数为：

\[
m_i=\tau_t\left[
\cos(f_i,t^{abnormal})-
\cos(f_i,t^{normal})
\right].
\]

文本锚点在训练过程中保持固定，不使用 Text Adapter、Prompt Learning 或在线 LLM
调用，从而减少文本语义对源训练数据的过拟合。

## 3. 空间—频率图建模

V3.1 将 Patch 特征恢复为空间网格，并为每个 Patch 建立固定八邻域连接。局部图传播
同时考虑 Patch 语义相似性、低频结构一致性和邻域异常状态，使可靠的局部结构能够修正
孤立且不稳定的异常响应。

在频率分支中，使用固定的平稳 Haar 小波对 Patch 特征网格进行分解，获得：

- 低频结构特征，用于描述局部组织和整体结构；
- 水平高频响应；
- 垂直高频响应；
- 对角高频响应。

空间邻域、文本异常概率和频率描述共同输入轻量图 Head。模型只有在异常语义、空间
关系和频率变化相互支持时才增强异常响应，从而降低将成像噪声直接判断为病灶的风险。

## 4. 有界残差异常修正

图 Head 不重新生成异常分数，而是在冻结 CLIP 的文本语义 Margin 上学习残差修正：

\[
z_i=m_i+\Delta_i.
\]

其中，\(\Delta_i\) 由空间—频率图特征预测，并受到最大修正幅度约束。该设计保留
CLIP 原有的正常—异常语义方向，同时限制小规模医学训练数据对预训练语义空间的过度
修改。

最终 Patch 异常概率为：

\[
P_i=\sigma(z_i).
\]

图像级分数使用三种互补证据。Top-k 分支保留小病灶的局部高响应，GeM 分支描述较大
范围或弥漫异常，冻结 CLIP 最终 CLS 则提供全局语义证据：

\[
P_{local}=\sum_l w_l\left[
\beta P_{topk}^{(l)}+(1-\beta)P_{gem}^{(l)}
\right],
\qquad
P_{img}=\alpha P_{cls}+(1-\alpha)P_{local}.
\]

默认 Top-k 比例为 5%，GeM 指数为 3。\(\alpha\) 和 \(\beta\) 由训练源学习，初始化
为 0.5 并限制在 `[0.1,0.9]`，所以全局/局部和局灶/弥漫分支都不会被完全抛弃。
CLS 只影响 Image 分数，不参与 Pixel 异常图融合。

## 5. 病灶保持频带训练

为了提高模型对频率变化的稳定性，训练时随机选择一个高频方向，并在给定区间内对其
进行缩放。基础频率特征产生基础异常图，扰动后的频率特征产生辅助异常图。

正常区域使用频带一致性损失，要求频率变化前后的异常预测保持稳定：

\[
L_{normal}=\frac{
\sum_i(1-M_i)\left(P_i-\widetilde{P}_i\right)^2
}{
\sum_i(1-M_i)+\epsilon
}.
\]

有病灶 Mask 的异常区域使用单边病灶保持损失，只惩罚频率干预造成的病灶响应下降：

\[
L_{lesion}=\frac{
\sum_iM_i\max(0,P_i-\widetilde{P}_i)
}{
\sum_iM_i+\epsilon
}.
\]

因此，模型在正常区域学习对频率扰动保持稳定，同时不会为了追求一致性而抹除真实
病灶。

## 6. 训练目标

V3.1 的总损失为：

\[
L=L_{pixel}+\lambda_{img}L_{image}
+\lambda_{normal}L_{normal}
+\lambda_{lesion}L_{lesion}.
\]

其中，\(L_{pixel}\) 为 Pixel Focal/Dice 损失，\(L_{image}\) 为图像级二分类损失。
对于没有病灶 Mask 的异常图像，只使用图像级监督，不使用全零伪 Mask 参与 Pixel、
正常区域一致性或病灶保持损失。

## 7. 轻量单阶段优化

V3.1 完全冻结 CLIP Image Encoder 和 CLIP Text Encoder，仅优化空间—频率图 Head、
四个分层权重和两个池化混合权重：

```text
模型总参数：428,782,210
可训练参数：约 25,991
可训练比例：约 0.0061%
```

整个方法只有一个正式训练阶段，不需要预先训练文本模块。固定文本锚点为图 Head 提供
异常语义方向，图 Head 则学习结合医学图像的空间结构与频率特征修正 Patch 异常响应。

## 8. 方法总结

V3.1 以冻结 CLIP 的正常—异常文本语义为基础，通过固定八邻域空间图和平稳 Haar
频率描述建模医学 Patch 之间的局部关系，并使用有界残差修正获得异常图。病灶保持
频带训练在提高正常区域稳定性的同时，避免频率干预削弱真实病灶响应。

一句话概括：

> V3.1 以固定医学文本语义为异常先验，通过病灶保持的空间—频率图传播修正冻结
> CLIP 的 Patch 异常响应，在抑制噪声伪异常的同时保留真实病灶。
